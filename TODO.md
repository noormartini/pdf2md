# TODO

## Experiment & Evaluation

- [ ] **Hand-write reference files for HITL thesis** (`references/A_human_in_the_loop_.../`)
      The 10 files there now were auto-generated by the same pipeline being evaluated.
      They need to be manually corrected to be an honest ground truth.

- [ ] **Hand-write reference files for Koehler thesis** (`references/Bachelor_Thesis_Informatik_Koehler_Sven/`)
      Same issue — auto-generated, need manual correction.
      Note: page_004.md and page_006.md are blank pages — leave them as-is.

- [ ] **Add PDFs from 3 missing document categories**
      Currently all 3 test PDFs are academic theses. RQ5 requires testing across:
      - Administrative forms
      - Presentation slides
      - Scanned archival documents
      For each new PDF, create a matching `references/<pdf-stem>/` folder with
      hand-written Markdown references and add the PDF to `experiments/sample.json`
      with the correct `"category"` label.

- [ ] **Run the full experiment**
      With LM Studio running: `python3 -m evaluation.compare -c experiments/sample.json -o output/results.json`
      Then generate the report: `python3 -m evaluation.report -i output/results.json -o output/report.md`

---

## Postprocessing (Hard — deferred)

- [ ] **Subscript and inline math artefacts**
      Expressions like `( _s_ + 1) _/_ 2` and `[ _−_ 1 _,_ 1]` are mangled because
      pymupdf4llm renders italic/superscript characters as Markdown underscores.
      No safe regex fix — needs math context awareness or a vision LLM pass.

- [ ] **Figures entirely missing**
      Every diagram and chart is replaced with `==> picture [NxN] intentionally omitted <==`.
      Recovering figure content requires a vision LLM to process the rendered page image.
      This is out of scope for the text-extraction path.

---

## Thesis Writing

- [ ] **Chapter 5 — System Design and Implementation**
      Section 5.1 (Architecture Overview) is bullet points only.
      Section 5.2 (Page Content Classifier) has one paragraph — needs expanding.
      Sections 5.3–5.6 are all bullet points.

- [ ] **Chapter 6 — Results and Evaluation**
      Cannot be written until the experiment has been run and results are available.

- [ ] **Chapter 7 — Discussion**
      Cannot be written until Chapter 6 is done.

- [ ] **Chapter 8 — Conclusion**
      Last chapter — written after everything else.

---

## Minor / Nice to Have

- [ ] **Verify token_usage for image and hybrid strategies**
      After running a real experiment with LM Studio, check whether `token_usage`
      is still null for image/hybrid results. LM Studio should return usage in the
      response but this has not been confirmed with a live run.

- [ ] **Add PDFs for other categories to sample.json**
      Once the new PDFs and references are ready, update `experiments/sample.json`
      to include them with correct category labels (forms, slides, scanned).
